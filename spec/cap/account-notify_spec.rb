require 'spec_helper'

describe 'CAP account-notify' do
#  before(:each) do
#    @swarm = Ircfly::Swarm.new
#    server = IRC_CONFIG.servers['primary']
#    @obot = @swarm.fly(server: server.host, port: server.port, nick: 'obot')
#    @cbot1 = @swarm.fly(server: server.host, port: server.port, nick: 'cbot1')
#  end

  it 'should show account changes if enabled'

  it 'should not show account changes if not enabled'

end
